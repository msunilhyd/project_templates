import { makeStyles } from '@material-ui/core';

export default makeStyles((theme) =>  ({
  containerPrimary:{
          margin: 0,
          padding: theme.spacing(1),
          border: 0,
          minWidth: '100%',
          maxWidth: '100%',

        },
   
}));