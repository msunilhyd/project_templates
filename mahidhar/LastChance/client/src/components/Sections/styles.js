import { makeStyles } from '@material-ui/core/styles';


export default makeStyles(() => ({
   
    toolbarSecondary: {
      justifyContent: 'space-between',
      overflowX: 'auto',
      maxWidth: 'lg',
    }
    
  }));